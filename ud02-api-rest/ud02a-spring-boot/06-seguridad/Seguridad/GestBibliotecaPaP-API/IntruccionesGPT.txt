sigue mis instrucciones, lo vamos a hacer paso a paso, para poder explicarlo a los estudiantes:
1º Modelo entidad relación, que ya está hecho,
2º Crear el proyecto springintlzr
3º Descargarlo, abrirlo en el IDE (inteliJ o eclips u otro), configurar base de datos (ahora h2, luego mysql o postgresql en contenedor docker) (alternativamente se puede hacer en ide diractamente como Intellij o eclipse)
4º Crear estructura del proyecto paquetes config, controladores, servicios (aunque no los usaremos hasta refactorizar), repositorios, entidades,(explicamos entidades, repositorios, controladoers, servicios y los principales parámetros de la configuración
(explicar el porqué de esa estructura y alguna estructura alternativa)
5º Ahora ya sí creamos las entidades, de paso explicamos el proyecto lombok
(las anotaciones lombok y las anotaciones jpa)
5º.1) Explicamos que es un repositorio de donde sale, los tipos, y vamos a añadiéndolos
6º )Explicamos los controladors, los endpoints, las varias formas de hacerlos
6º.1) Una vez sabemos los endpoints que necesitamos vamos añadiendo los test unitarios junit 5.8 u otro (explicando si usamos otra librería) para hacer TDD (primero los test, luego la función)
6º.2) y los vamos añadiendo, explicando las distintas anotaciones
7º) Añadimos paginación y filtros (podemos hacer algunos en repositorios y otros en otra parte)
(explicando cómo lo añadimos y qué es cada cosa)

8) Autenticación con JWT,
9) Pruebas de integración, mejorar las pruebas unitarias

10)refactorizar controladores y separar la parte de acción en los servicios (expicar los serivicios)
11) añadir la lógica de negocio que falte de gestión de libros, préstamos, socios...
