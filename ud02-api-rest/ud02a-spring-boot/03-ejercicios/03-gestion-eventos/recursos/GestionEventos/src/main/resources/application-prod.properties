# Perfil: producción (prod)

# Configura aquí tu BD real (ejemplos comentados). Elige uno y ajústalo.

## PostgreSQL
# spring.datasource.url=jdbc:postgresql://localhost:5432/gestioneventos
# spring.datasource.username=usuario
# spring.datasource.password=contraseña
# spring.datasource.driver-class-name=org.postgresql.Driver

## MySQL/MariaDB
# spring.datasource.url=jdbc:mysql://localhost:3306/gestioneventos?useSSL=false&serverTimezone=UTC
# spring.datasource.username=usuario
# spring.datasource.password=contraseña
# spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA/Hibernate
spring.jpa.hibernate.ddl-auto=none
spring.sql.init.mode=never
spring.h2.console.enabled=false
spring.jpa.open-in-view=false

# Logging más conservador
logging.level.root=INFO
logging.level.org.springframework=INFO
