spring.application.name=GestionEventos
# Archivo común (sin perfil): configura lo compartido por todos los entornos.

# PERFIL ACTIVO POR DEFECTO (puedes cambiarlo aquí si compilas/ejecutas desde el IDE):
# - Cambia el valor a dev | test | prod según necesites.
# - Este valor puede ser sobrescrito por variable de entorno SPRING_PROFILES_ACTIVE
#   o por parámetro de arranque: -Dspring.profiles.active=...
spring.profiles.active=dev

# Ajustes comunes opcionales (añade aquí lo que deba aplicar a todos los perfiles)
# logging.level.root=INFO
