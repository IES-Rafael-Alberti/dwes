# Ficheros con NIO.2

Ejemplo mínimo de `Path` + `Files` para escribir y leer texto UTF-8. Es la
base que después se reutiliza al guardar uploads o preparar descargas en una
aplicación web.

```bash
javac -d out src/*.java
java -cp out Main
```
