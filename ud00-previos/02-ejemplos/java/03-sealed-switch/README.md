# Sealed y switch

Ejemplo de una jerarquía cerrada con `sealed`, records y `switch` como
expresión. Requiere JDK 25 por el uso de una guarda `when`.

```bash
javac -d out src/*.java
java -cp out Main
```
