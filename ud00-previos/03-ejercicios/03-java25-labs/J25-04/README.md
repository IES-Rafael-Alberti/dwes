# J25-04 - Ultimos titulos

Implementa `CollectionQueries.latestTitles`.

- Ordena por fecha descendente.
- Devuelve como maximo `limit` titulos.
- Ignora notas con titulo vacio despues de hacer `strip()`.
- No modifiques la lista recibida.

Primero resuelvelo con `sorted` y `limit`. Despues razona si `toList()` es una
lista modificable o no y por que eso importa en una API.
